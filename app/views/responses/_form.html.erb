<div>
	Replies:
</div>
<%= form_for [@question, @response = Response.new] do |f| %>
	<!-- add error checking -->
	<div class="field">
		<%= f.text_area :response, rows: 3, class: 'span5' %>
	</div>
	<div class="field">
		<%= f.submit class: 'btn' %>
	</div>
<% end %>
